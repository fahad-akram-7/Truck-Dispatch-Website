{
  "private": true,
  "packageManager": "npm@10",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "npm exec --workspace=frontend vite dev",
    "build": "npm exec --workspace=frontend vite build",
    "build:frontend": "npm exec --workspace=frontend vite build",
    "start:backend": "npm exec --workspace=backend node src/server.js"
  }
}
